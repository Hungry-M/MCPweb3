# TRON MCP Server 环境配置 (Nile 测试网)

# TRONSCAN API URL (可选)
# 默认: https://apilist.nile.tronscanapi.com/api
# TRONSCAN_API_URL=https://apilist.nile.tronscanapi.com/api

# TRONSCAN API KEY (可选)
# 若有配额限制，可填写
# TRONSCAN_API_KEY=your_tronscan_api_key

# 请求超时时间 (秒，可选，默认 10)
# REQUEST_TIMEOUT=10

# SSE 模式端口 (可选，默认 8765)
# MCP_PORT=8765
